#ifndef VERSION_H
#define VERSION_H

#include <QtGlobal>

#define COMPANY_NAME "G2Metric"
#define ORGANIZATION_DOMAINE "https://www.g2metric.com/"
#define APPLICATION_FULL_VERSION "${GIT_SEMVER}.${GIT_COMMIT_COUNT}"
#define VERSION_COUNTER ${GIT_COMMIT_COUNT}
#define COMMIT_HASH "${GIT_DESCRIBE}"
#define APPLICATION_VERSION "${GIT_SEMVER}"
#define APPLICATION_NAME "${PROJECT_NAME}"
#define COMMIT_AUTHOR "${COMMIT_AUTHOR}"
#define COMMIT_DATE "${COMMIT_DATE}"
#define CURRENT_BRANCH "${CURRENT_BRANCH}"
#define COMPILATION_AUTHOR "${COMPILATION_AUTHOR}"
#define COMPILATION_DATE "${COMPILATION_DATE}"
#define COMPILER_NAME "${COMPILER_NAME}"

#endif  // VERSION_H
